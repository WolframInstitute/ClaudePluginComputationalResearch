Create a Paper/ folder with LaTeX templates using the `paper-init` skill.

If inside a project directory, create Paper/ here. Otherwise ask where.
Uses amsart, biblatex with biber, and a shared macros.sty preamble.
If Wiki/Resources/ exists, seed references.bib from existing papers.
