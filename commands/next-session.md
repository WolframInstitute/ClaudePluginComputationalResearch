Run one disciplined work session using the `next-session` skill.

Pick the next incomplete task from the named work item (or the single active one,
or ask), study its Spec, implement EXACTLY ONE task, append a Progress report, mark
the task done, sync the Wiki, commit, and STOP.

Run this in a FRESH session each time — clean context per task is the whole point.
Example: `/next-session GraphCurvature`
