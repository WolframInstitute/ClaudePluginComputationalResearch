Create or update a plan using the `wiki-plan` skill.

If arguments are provided (e.g., `/plan migration to new API`), create a
plan for that topic. If no arguments, list existing plans in Wiki/Plans/
and ask what to work on.

Plans follow the revision workflow: draft, present, wait for feedback.
