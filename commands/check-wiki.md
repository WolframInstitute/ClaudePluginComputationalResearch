Run a wiki health check using the `check-wiki` skill.

Scan for stale articles, missing coverage, broken links, missing index entries, and resource issues.
Fix what can be fixed automatically and report what needs human attention.
