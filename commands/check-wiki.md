Run a wiki health check using the `wiki-health` skill.

Scan for stale articles, missing coverage, broken links, missing index
entries, and resource issues. Fix what can be fixed automatically and
report what needs human attention.
