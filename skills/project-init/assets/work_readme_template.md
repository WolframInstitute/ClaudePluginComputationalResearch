# Work

Active work for {{PROJECT_NAME}}. Each file here is one **work item** — a
multi-session effort with a Spec (what to build), Tasks (one ≈ one session), and
Progress (per-session reports). This is execution state; durable knowledge lives
in `Wiki/`.

Run `/next-session` in a **fresh** session to work the next task of an active
item — clean context per task is the whole point. Use `/work` to create a new
item or to see this board.

## Board

| Item | Status | Next task |
|---|---|---|
| _(none yet)_ | | |
