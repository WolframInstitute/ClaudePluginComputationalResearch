# Routing trial

*[ LLM Generated ]*

Scratch evidence for `ModelRouting` T3, to be harvested and deleted.

- 2026-08-20, T1, haiku, high
- 2026-08-20, T2, sonnet, high

T1's bullet names `haiku`; this session's own system prompt names `sonnet`.
The two sessions ran on different tiers within one driver run, so the driver routed per task rather than per run — the annotation reached a live `claude -p` call and was not merely a stub.
